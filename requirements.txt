# 视觉识别虚拟环境的 Python 依赖（Python 3.10）
#
# 使用方式：先装 PyTorch（机器相关，见 setup.sh 或 PyTorch 官网），再：
#   python3 -m venv vision_env
#   source vision_env/bin/activate
#   pip install -r requirements.txt
#
# torch / torchvision / torchaudio 不在此文件里（CPU/CUDA 版本因机器而异），
# 由 setup.sh 按 PyTorch 官网命令安装。

# ── ROS 2 Python 绑定（与系统 ROS Humble 的 message 定义对齐） ──
# 用 pip 版独立安装，避免依赖系统 ROS 的 site-packages。
rclpy==3.3.20
geometry-msgs==4.9.1
nav2-msgs==1.1.20
sensor-msgs==4.9.1
visualization-msgs==4.9.1
tf2-ros-py==0.25.19
tf2-geometry-msgs==0.25.19
tf2-sensor-msgs==0.25.19

# ── 开放集检测 + 实例分割 ──
# 注意：装的是 groundingdino-py（PyPI 打包版，可用），不是 upstream 仓库里那版
# groundingdino 0.1.0（其 ms_deform_attn.py 与 transformer.py 不匹配，推理报错）。
groundingdino-py==0.4.0
supervision==0.6.0
transformers==4.28.1

# ── 数值 / 图像 ──
# numpy 2.x 与 ROS 的 cv_bridge ABI 不兼容，脚本已改用手动 numpy 解码，勿降级。
numpy==2.2.6
opencv-python==5.0.0.93
pillow==12.2.0
matplotlib==3.10.9

# ── INSID3（DINOv3 骨干复核）依赖 ──
hydra-core==1.3.4
omegaconf==2.3.1
iopath==0.1.10
einops==0.8.2
scikit-learn==1.7.2
pycocotools==2.0.11
timm==1.0.28

# ── 其它运行时依赖 ──
tqdm==4.70.0
termcolor==3.3.0
